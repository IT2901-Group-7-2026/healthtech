name: healthtech-prd

services:
  traefik:
    image: traefik:v3.0
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./infra/traefik/traefik.yml:/traefik.yml:ro
    networks:
      - app-network

  db:
    image: timescale/timescaledb:latest-pg17
    restart: unless-stopped
    env_file:
      - ~/env/backend.env
    volumes:
      - timescale_data:/var/lib/postgresql/data
      - ./backend/seed:/seed
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    env_file:
      - ~/env/backend.env
    environment:
      # We override `Host` to use the docker container name defined above
      DATABASE_URL: "Host=db;Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=PathPrefix(`/api`)"
      - "traefik.http.routers.backend.entrypoints=web"
      - "traefik.http.services.backend.loadbalancer.server.port=8080"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ~/env/frontend.env
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=PathPrefix(`/`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.http.routers.frontend.priority=1"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"

  db-migrator:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # "efbundle" is created in backend/Dockerfile and is a self-contained executable for running EF Core migrations
    entrypoint: ["./efbundle"]
    command:
      [
        "--connection",
        "Host=db;Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}",
      ]
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/seed:/seed
    env_file:
      - ~/env/backend.env
    networks:
      - app-network
    restart: "no" # Don't restart after it finishes

volumes:
  timescale_data:

networks:
  app-network:
    driver: bridge
